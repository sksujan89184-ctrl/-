{
  "name": "workspace",
  "version": "1.0.0",
  "description": "This repository provides a minimal Android app skeleton for `Maya` — an assistant with speaker-verification gated actions, a PDF maker, and a Live Action Log. It includes a `system_prompt.json` you can use to seed your assistant's system prompt.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^1.35.0",
    "drizzle-zod": "^0.8.3",
    "p-limit": "^7.2.0",
    "p-retry": "^7.1.1",
    "zod": "^4.3.5",
    "zod-validation-error": "^5.0.0"
  }
}
